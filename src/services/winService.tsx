import axios from 'axios';
import constants from './serviceConstants';

const ID = 'id';

const getAllWins = () => {
  /*
    This needs to be completed.
  */
};

const getWin = () => {
  /*
    This needs to be completed.
  */
};

const createWin = ( ) => {

};

const updateWin = () => {
  // create a new user object with all the information
  //
  /*
    This needs to be completed.
  */

};

const deleteWin = () => {
  /*
    This needs to be completed.
  */
};

export default { getAllWins, getWin, createWin, updateWin, deleteWin };
